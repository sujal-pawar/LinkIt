# Screenshots needed here

I can't run a browser in this sandbox (no network access to install
dependencies or render the live app), so these are placeholders in the
README pointing at files that don't exist yet. Capture these on your
machine and drop them in this folder with these exact filenames, and
they'll appear in the README automatically:

| Filename | What to capture |
|---|---|
| `landing.png` | The join-room screen (the "hero" section) |
| `connected.png` | Both peers connected — the "P2P Connected" state |
| `transfer.png` | A file actively transferring, with the progress bar visible |
| `received.png` | A completed transfer with the download link showing |
| `mobile.png` | The landing screen on a real phone (this is a good one to include given the mobile perf fix — shows it actually working well on mobile) |

Tips for good screenshots:
- Use consistent browser window width (or your phone's real screen) for all of them
- Dark theme is the default — no need to change anything
- For `mobile.png`, use your phone's actual screenshot function for a real device shot, not just a resized browser window
